# Dev log

Notable changes between commits. Supplement git history with a short note here when you make significant changes.

---

- **Conducting cards and events (week→month→season→annual):** `docs/conducting_cards_and_events.md` — design for promoter, federation, world, wrestlers. `run_month`, `run_season` in orchestrator. `build_season` in month_builder. Month/season context in hints (`build_month_context`, `build_season_context`); promoter preamble includes week-of-month, phase (build_up_to_ppv, ppv_week, post_ppv_fallout). PPV attached to month in build_month.

- **Temporal plane: memory + gamification:** Tier 9 immutables — `models/memory.py` (ArchiveTier, Tier9CardRecord), `Tier9ImmutableDB`; `record_tier9_immutable()` appends card date, attendance, match card/results, title changes after each card. `recall()`, `recall_context_for_prompt()` for bounded historical context. RippleDB, TrapdoorDB; `create_ripple()`, `create_trapdoor()`; trapdoor on `set_conceptual_card` when main_event_target changes. Promoter hints include `run_state` (recent_ripples, recent_trapdoors) and `tier9_recall`.

- **Optionals + Phase 8:** Phase 2.3: FanReaction wired — after participant actions, `_update_fan_reaction` calls `FanEngagement.generate_fan_reaction`, stores `_last_fan_reaction`; crowd context and preamble include it. Phase 7.2: Promoter picks promo — `Card.promo_lineup: Optional[List[List[str]]]`; card_builder uses it for promo segments when set. Phase 8: Gate/revenue — `models/revenue.py` (RevenueResult, compute_card_revenue), `CardRevenueDB`, `agent_service/revenue_crud.py`, `compute_revenue=True` in orchestrator.

- **Simulation completion (Phases 4–7):** Phase 4: Day-before prep tick — SEGMENT_ROLES["prep"] = [backstage, promoter]; `_run_day_before_prep`; `run_prep=True` in run_full_card, run_week, run_week_from_template. Phase 5: Day-after fallout — SEGMENT_ROLES["fallout"]; `_run_day_after_fallout` with match_results_summary; `run_fallout=True`. Phase 6: Next-day coverage — MediaOutletDB, CoverageDB in db_models; `agent_service/coverage_crud.py` with `create_next_day_coverage`; `create_coverage=True` creates Coverage row (scope=next_day, published_at=card_date+1). Phase 7: Promo with wrestler — card_builder assigns next match's participants to PROMO segments; orchestrator passes promo_participant_ids in hints; engine runs those participants with "You are cutting a promo" context.

- **Simulation completion implementation:** Implemented Phases 1–3 from `docs/simulation_completion_plan.md`. Phase 1: `card_type_from_show_type(show_type)` in `simulation/card_builder.py`; `run_week_from_template` converts each Card to FullCard via `build_full_card` before running, so week shows run full segment flow (opening, promo, matches, commercial, backstage, closing). Phase 2: `PromptBuilder` adds crowd preamble with `favorite_agent_ids` and `hated_agent_ids` when present; `run_full_card` builds base hints (venue, audience, etc.) once and merges into segment_hints for all segments so non-match segments get audience bias. Phase 3: Verified SEGMENT_ROLES — commercial runs announcer only (crowd does not); segment_type in state. Updated plan checklist.

- **Week/month/life implementation (continued):** Added **fill_week_matches(db, week)** in `simulation/week_builder.py` — fills matches for each card using travel_squad_ids (or get_available_at_date fallback) via MatchScheduler._create_optimal_match_card. Added **run_week_from_template(federation_id, week_start_date, template=None, max_ticks_per_match=200, fill_matches=True)** in `simulation/orchestrator.py` — build_week_from_template → fill_week_matches → run_week. Exported fill_week_matches, run_week_from_template from simulation/__init__.py. Week flow: build week → fill matches → run all cards.

- **Week/month/life implementation:** Implemented design from `docs/week_month_and_life_design.md`. **Models**: `models/week_schedule.py` — ShowType, ShowSlot, WeekTemplate; default_standard_week_template, default_ppv_week_template. **DB**: WeekTemplateDB, AgentFatigueDB. **Card** extended with show_type, prep_date, travel_squad_ids. **Roster pools**: `simulation/travel_squad.py` — get_tv_roster (full_time/developmental or veteran+rising), get_house_roster (exclude ppv_appearance), get_ppv_roster (TV + ppv_appearance/legend). **Travel squad**: get_travel_squad(federation_id, card_date, show_type, loop A/B, exclude_high_fatigue). **Fatigue**: `core_engine/fatigue.py` — get_fatigue, increment_fatigue, FATIGUE_THRESHOLD_REST; travel squad excludes agents above threshold. **Week builder**: build_week_from_template(db, federation_id, week_start_date, template) → Week with Cards (card_date, show_type, prep_date, travel_squad_ids). **Month builder**: build_month(db, federation_id, year, month_number) → Month with 4–5 weeks, PPV week = last week. **Orchestrator**: after run_card/run_full_card, increment_fatigue once per card for all participants; _build_hints adds travel_squad_ids, prep_date, show_type. Exports in models/__init__.py and simulation/__init__.py.

- **Week, month, day-around-card, roster split, personal vs kayfabe (design):** Added `docs/week_month_and_life_design.md`. Design covers: **day before** a card (travel, prep, promo day; optional prep tick); **day after** (travel home, rest, media, critics, fatigue); **week structure** (WeekTemplate: day_of_week + show_type slots, how many house/TV/PPV per week); **roster split** (travel squad per show, house loop A/B, TV roster, PPV roster; who travels where and why—fatigue, tier, contract); **week + month** (month = 4–5 weeks, PPV week, build-up/fallout, generate cards + squads); **personal vs kayfabe** (existing WrestlerPersonality + POV; **fatigue** model: + on work, − on rest, travel squad excludes high fatigue). Implementation order suggested (template → roster pools → travel squad builder → fatigue → loop A/B → day before/after → month builder). Linked from CONTRIBUTING and calendar_concepts.md.

- **Full card: before match, match, after match + glue:** Built the whole card run with segment-by-segment execution and shared state. **CardRunState** (models/card_structure.py): segment_index, previous_segment_type, last_match_result (winner_id, match_id, participant_ids), segment_results; to_hint_dict() for context. **SEGMENT_ROLES** and **POV_TO_ROLES**: which roles run for each non-match segment (opening → announcer, crowd, backstage, promoter; promo, closing same; backstage → backstage, promoter; etc.). **Engine**: run_one_segment_tick(segment_type, card_type, card_run_state, hints) runs one tick per role for that segment; _build_segment_context and _process_agent_for_segment_tick. **Orchestrator** run_full_card: init CardRunState; for each segment merge card_run_state into hints; match segments run run_match then update last_match_result and segment_results; non-match segments (in SEGMENT_ROLES) run engine.run_one_segment_tick then append to segment_results; previous_segment_type updated after each. So opening/promo/backstage/closing run before/between/after matches and all segments see what came before. Doc: docs/multi_stream_cards.md §7.

- **Match in the middle + glue (venue, audience, viewing):** Built a match in the middle of a 3-match card and wired all context so it receives venue, audience (favorites/hated), and viewing_context. **FullCard** and **Card** now have `venue_id`; **card_builder** passes it through. **Orchestrator** `_build_hints`: loads **venue** (via `agent_service/venue_crud.py`: get_venue, venue_to_hint), **audience** (AudienceSegmentDB by card_id: venue_type, fan mix, favorite_agent_ids, hated_agent_ids; else defaults), **viewing_context** (default "arena"). **Demo cards**: `build_demo_card` and new `build_demo_card_three_matches` use `ensure_default_venue` so cards have a venue. **Script** `scripts/run_middle_match.py`: builds 3-match card, full card, gets middle segment, builds hints, prints what the match has; optional `--run` to execute a few ticks. Doc: `docs/world_bubbles.md` §8 (match-in-the-middle: what a match receives).

- **World bubbles (venues, critics, media, viewing, fan fav/hated):** Added `docs/world_bubbles.md` — venues (place, capacity, concessions, PPV, gate); critics and media (next-day stories, real-time, blogs); viewing microcosms (pub, living room, newsroom); fan favorites/hated. Added `models/venue.py` (Venue, VenueType). Added `models/media.py` (MediaOutlet, OutletType, Critic, Coverage, CoverageScope). Extended `models/audience.py` with ViewingContext, ViewingContextType, FanPreferences; AudienceSegment now has favorite_agent_ids, hated_agent_ids. Card has venue_id. VenueDB and audience_segments.favorite_agent_ids/hated_agent_ids in db_models; migration in agent_service/database.py. Exports in models/__init__.py.

- **One card structures and guidance:** Exported `build_anchor_card` from `simulation/__init__.py`. Added §10 "The One Card (Build)" to `docs/world_anchor_framework.md` — anchor card builder, conceptual target, promoter guidance, API. Added "Simulation: One Card and World Anchor" section to `CONTRIBUTING.md` — key paths, workflow, API endpoints for contributors.

- **Anchor card build:** Implemented the one coherent card for the marquee show. Added `simulation/anchor_card_builder.py`: `build_anchor_card(db, federation_id, anchor, conceptual_target)` builds FullCard for anchor date using available roster, tenure mix (veteran main event, rising title matches, newcomer filler), titles, storylines with payoff_phase=anchor. Added `agent_service/anchor_crud.py`: `get_conceptual_card`, `set_conceptual_card` for target main event, title matches, planned storyline payoffs. Added `ConceptualCardDB` to `models/db_models.py`. Added `core_engine/promoter_guidance.py`: `build_promoter_guidance`, `build_anchor_stakes_hint` for LLM context. Orchestrator `_build_hints` now injects `world_anchor`, `conceptual_card`, `promoter_guidance`, `anchor_stakes`. PromptBuilder adds promoter-specific preamble with guidance when role=promoter. API: GET `/federations/{id}/anchor_card` uses build_anchor_card; POST/GET `/federations/{id}/conceptual_card` for target. Migration for `storylines.payoff_phase`.

- **Two temporals (conceptual vs run):** Documented and modeled the duality: **conceptual timeline** (world built to achieve the card—plan, target) vs **run timeline** (reality for the promoter in the moment—pieces introduced over time, sorted from chaff). Added §9 to `docs/world_anchor_framework.md`: gamification layer (random chance, multiple branches, pruning), trapdoors (change of mind, swerves), ripples and effects (injury, cold start, lady luck). Added `models/temporals.py`: TemporalLayer, BranchStatus, RippleCause, Ripple, Trapdoor, RunState, ConceptualCard. Engine can distinguish plan-from-reality and support chance, branches, trapdoors, ripples.

- **World anchor depth (roster, tenure, injuries, betrayals):** Not every wrestler on the 2-year show is on the roster today. Added roster-over-time design and implementation: **TenureTier** (veteran, rising, newcomer) and **Injury** (out_from, out_until, return_surprise) in `models/roster.py`. **InjuryDB** for persistence. **StorylinePayoffPhase** and **Storyline.payoff_phase** (build_up, anchor, aftermath) in `models/wrestling.py`; **StorylineDB.payoff_phase**. **simulation/roster_timeline.py**: `get_roster_at_date`, `get_available_at_date` (minus injured), `tenure_tier_for`, `get_tenure_mix_at_date`, `get_anchor_card_composition` (tenure mix, out_injured, just_returned for marquee). Doc: `docs/world_anchor_framework.md` §8 (roster churn, tenure mix, injuries, surprises, betrayals; anchor card composition). Enables veterans/rising/newcomer mix, injuries and comebacks, payoff-phase planning for storylines.

- **World anchor framework (4-year spine):** Anchors the engine on one marquee annual show two years in the future. Added `docs/world_anchor_framework.md` (backward/forward design, promoter achievement, gamification). Added `models/world_anchor.py`: WorldAnchor (world_start_date, anchor_event_name, anchor_date=+2y), WorldPhase (build_up, anchor, aftermath), phase_for(), weeks_until_marquee(), years_from_anchor(), AnchorMilestone, DEFAULT_ANCHOR_MILESTONES, build_default_anchor(). Added WorldAnchorDB. Orchestrator _build_hints() injects world_anchor (phase, weeks_until_marquee, etc.) for promoter/LLM context. API: GET/POST /federations/{id}/world_anchor, GET /federations/{id}/anchor_card (prospective marquee card for anchor date).

- **ENHANCEMENT_PROPOSAL implementation:** Phase 1.2: EventBroadcaster (api_gateway/websocket.py), WebSocket /live/federation/{id}. Phase 2.1: NarrativeEngine (core_engine/narrative/), MatchNarrative. Phase 2.2: MatchScheduler (core_engine/scheduling/), schedule_weekly_show, POST /scheduling/weekly. Phase 2.3: CharacterEvolution (core_engine/character_evolution.py), WrestlerStatsDB, WrestlerPersonalityDB updates after match; orchestrator calls CharacterEvolution instead of _update_agent_records. Phase 3.1: FanEngagement (core_engine/fan_engagement.py), Poll, Vote, FanReaction. Phase 3.2: MediaGenerator (core_engine/media_generator.py), HighlightReel, WeeklyRecap. Phase 4.1: AgentMemory (core_engine/agent_memory.py), recall_relevant_events. Phase 4.2: StorylineDirector (core_engine/storyline_director.py), orchestrate_federation. API: GET /agents/{id}/stats for wrestler stats. See docs/ENHANCEMENT_PROPOSAL.md.

- **Roster, contracts, staff, audience:** Added `models/roster.py` — Roster, Contract, ContractType, ContractStatus, WrestlerStats, WrestlerPersonality. Added `models/staff.py` — AnnouncerProfile, AnnouncerType, RefereeProfile, ManagerProfile, ValetProfile. Added `models/audience.py` — FanType (superfan, super_viewer, common_viewer, common_fan), AudienceDemographics, AudienceSegment. DB tables: ContractDB, WrestlerStatsDB, WrestlerPersonalityDB, StaffProfileDB, AudienceDemographicsDB, AudienceSegmentDB. Extended VALID_ROLES with valet, manager; ROLE_TICK_CADENCE and POV mapping for both. See `docs/roster_staff_audience.md`.

- **Full card + multi-stream POV:** Added `models/card_structure.py` — CardType (house, minor_tv, major_tv, ppv, marquee_season, marquee_year), POV (ring, tv, crowd, backstage, promoter), Segment, FullCard, SEGMENT_POV_VISIBILITY, CARD_TYPE_SEGMENT_TEMPLATE. Added `simulation/card_builder.py` — `build_full_card(card, card_type)` maps matches into segment slots per template. Orchestrator: `run_full_card(full_card)` runs segment-by-segment; match/dark_match call `run_match`; opening/promo/backstage/commercial/closing log and skip. Engine: `set_segment_context(segment_type, card_type)`, `_build_context` derives POV from role, adds `pov`, `segment_type`, `card_type` to state. `build_demo_full_card(federation_id, card_type)`. See `docs/multi_stream_cards.md`.

- **Completed remaining items:** Title/Storyline CRUD: `create_title`, `create_storyline`, `get_titles`, `get_storylines` in `agent_service/wrestling_crud.py`. API endpoints: GET/POST `/titles`, GET `/titles/{id}`, GET `/titles/{id}/champion`; GET/POST `/storylines`, GET `/storylines/{id}`. Parallel participant LLM calls: `_process_participants_parallel()` uses `ThreadPoolExecutor` to run participant LLM calls in parallel when 2+ participants, then applies effects sequentially.

- **Plan implementation + LLM consolidation:** Winner detection: engine sets `_last_finisher_agent_id` when finisher ends match; orchestrator reads via `get_last_finisher_agent_id()` and sets `winner_id`. Opponent context: engine `set_match_context(participant_ids)`; `_build_context` derives `opponent_id` and `opponent_style`, passes to `MoveEngine.get_available_moves()`. Title CRUD: `agent_service/wrestling_crud.py` (get_title_by_id, get_current_champion, start_reign); orchestrator updates ReignDB on title change. Storyline hints: orchestrator `_build_hints()` loads storyline and title context into promoter hints. Heat integration: orchestrator uses `update_wrestler_heat` from heat.py. Agent update whitelist: added `current_heat`, `momentum`, `win_streak`, `loss_streak`, `alignment` to `_AGENT_UPDATE_FIELDS` and `AgentUpdateData`. LLM consolidation: `llm_abstraction/unified.py` — `UnifiedLLMClient` with `send_prompt()`; engine uses `get_unified_llm()`; `core_engine/llm_client` re-exports for backward compat.

- **Match engine (move pool):** Added `core_engine/move_engine.py` with 50+ real wrestling moves (common strikes/grapples, special signatures, finishers). `MoveEngine.get_available_moves()` filters by position (standing/ground/corner/ropes/top_rope), momentum, style (powerhouse/technical/high_flyer/brawler/submission). Wrestlers draw from pool based on gimmick-derived style and tick-derived position. Finisher detection via `MoveEngine.is_finisher()`. Engine uses move pool for participant role instead of dispatcher._ACTIONS.

- **End-to-end simulation:** Added `models/wrestling.py` (Title, Reign, Team, Stable, Storyline, MatchResult, Alignment); extended `Match` with `title_id`, `storyline_id`, `is_title_match`; added DB tables (titles, reigns, teams, stables, storylines, match_results) and agent columns (alignment, win_streak, loss_streak). `SimulationOrchestrator` runs cards: pre_match → match → post_match per match, persists MatchResultDB, updates agent heat/win/loss. CLI: `python scripts/run_simulation.py [--card|--week] [--ticks N] [--dry-run]`. API: POST `/simulation/run`. Added migration for new agent columns.

- **Wrestling concepts design:** Added `docs/wrestling_concepts.md` — heat (babyface/heel, feud heat), teams, stables, titles (lineage, reigns), momentum (streaks, tiers), storylines, and **memories + 100-year scaling** (archive tiers, who remembers what, invariants for title lineage and records).

- **Match phase flow:** Implemented pre_match, match, post_match in engine. `run_pre_match()` runs promoter+backstage once; `run_post_match(match_result)` runs after finisher; `run_full_match(max_ticks)` runs full flow: pre→match→post. Added `PRE_MATCH_ROLES`/`POST_MATCH_ROLES` in `models/calendar.py`. Documented Card structure and match flow in `docs/calendar_concepts.md`. Cross-checked: no card-building code in other branches; `MatchScheduler`/`_create_optimal_match_card` exist only as proposals in `ENHANCEMENT_PROPOSAL.md`.

- **Calendar hierarchy extended:** Weeks fall into monthly cycles that culminate with PPVs; multiple months make up a season; multiple seasons make up a year. Added `Month`, `Season`, `Year` to `models/calendar.py`; updated `Week` with `month_id`, `PPV` with `month_id`; `Month` has `ppv` and `weeks`; `Calendar` has `years` (legacy `weeks`/`ppvs` kept). Updated `docs/calendar_concepts.md` and model exports.

- **Calendar concepts and test fix:** Added `docs/calendar_concepts.md` describing match phases (pre_match, match, post_match), per-role tick cadence, and calendar hierarchy (Card → Week → PPV → Calendar). Exported calendar types from `models/__init__.py`. Fixed engine test: mock `send_prompt` now accepts `model=None` argument to match engine API.

- **Root reorg:** Moved long-form docs to `docs/` (ANALYSIS, USAGE_GUIDE, ENHANCEMENT_PROPOSAL, codebase, security, implementation summaries, etc.) and demo/helper scripts to `scripts/` (demo.py, demo_multi.py, activate_env.sh). Updated README, AGENTS.md, and in-repo links accordingly.

- **Project review cleanup:** Added AGENTS.md (layout and run commands), DEVLOG.md, Pydantic v2 fix, pyproject.toml for editable install, engine DB session per tick, README frontend clarification.
